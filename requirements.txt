# Agent_Trader -- Avito stack
# GitHub Actions + Streamlit Cloud

# Data fetching
# curl_cffi: TLS browser impersonation for Stooq (Chrome/Safari/Firefox rotation)
# yfinance:  fallback data source
# requests:  plain HTTP for non-fingerprinted calls
pandas==2.2.3
numpy==1.26.4
requests==2.32.3
yfinance==0.2.54
curl_cffi==0.6.4

# Encryption + GitHub API
cryptography==42.0.8

# Dashboard
streamlit==1.40.0
plotly==5.24.1

# Excel upload
openpyxl==3.1.5
xlrd==2.0.1

# Utilities
python-dateutil==2.9.0
pytz==2024.2
